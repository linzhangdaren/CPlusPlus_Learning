//比较运算符
//等于==、不等于!=、小于<、大于>、小于等于<=、大于等于>=
//注意 需要加括号 真值为1 否值为0

#include<iostream>
using namespace std;

int main()
{
	int a = 10;
	int b = 20;

	cout << (a == b) << endl;//因为a不等于b所以会输出否定值为:0

	system("pause");
	return 0;
}
